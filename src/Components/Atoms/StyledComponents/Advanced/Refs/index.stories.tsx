import * as React from "react";
import { storiesOf } from "@storybook/react";
import { Form } from "./index";

storiesOf("StyledComponents/Advanced/Refs", module).add(
  "default",
  () => (
    <Form />
  )
);